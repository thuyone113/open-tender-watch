<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <title><%= yield(:title).presence || t("app.name") %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <!-- Google Tag Manager -->
    <script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({"gtm.start":
    new Date().getTime(),event:"gtm.js"});var f=d.getElementsByTagName(s)[0],
    j=d.createElement(s),dl=l!="dataLayer" ? "&l="+l : "";j.async=true;j.src=
    "https://www.googletagmanager.com/gtm.js?id="+i+dl;f.parentNode.insertBefore(j,f);
    })(window,document,"script","dataLayer","GTM-MMRZD3DR");</script>
    <!-- End Google Tag Manager -->
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600;700&family=Space+Grotesk:wght@400;500;600;700&display=swap" rel="stylesheet">
  </head>

  <body class="bg-[#0d0f14] text-[#e8e0d4] font-sans relative overflow-x-hidden min-h-screen">
    <!-- Google Tag Manager (noscript) -->
    <noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-MMRZD3DR"
    height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
    <!-- End Google Tag Manager (noscript) -->
    <style>
      @keyframes fadeUp {
        from { opacity: 0; transform: translateY(12px); }
        to { opacity: 1; transform: translateY(0); }
      }
      @keyframes pulse {
        0%, 100% { opacity: 1; }
        50% { opacity: 0.4; }
      }
      @keyframes scanline {
        0% { transform: translateY(-100%); }
        100% { transform: translateY(100vh); }
      }
      .animate-fade-up { animation: fadeUp 0.6s ease both; }
      .animate-pulse-slow { animation: pulse 2s ease infinite; }
    </style>

    <%# Scanline effect %>
    <div class="fixed top-0 left-0 right-0 h-0.5 bg-linear-to-r from-transparent via-[#ff444426] to-transparent pointer-events-none z-100" style="animation: scanline 4s linear infinite;"></div>

    <%# Grid overlay %>
    <div class="fixed inset-0 pointer-events-none z-0" style="background-image: linear-gradient(rgba(255,255,255,0.015) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,0.015) 1px, transparent 1px); background-size: 60px 60px;"></div>

    <%# Language picker — fixed top-right %>
    <div class="fixed top-4 right-6 z-50 flex items-center gap-1.5">
      <% [["en", "gb"], ["pt", "pt"]].each do |locale_code, flag_code| %>
        <% active = I18n.locale.to_s == locale_code %>
        <%= link_to set_locale_path(locale: locale_code),
            class: "flex items-center gap-1.5 px-2 py-1 rounded border transition-all #{active ? 'border-[#c8a84e55] bg-[#c8a84e0d]' : 'border-white/[0.08] bg-white/[0.02] hover:border-white/20 opacity-50 hover:opacity-100'}" do %>
          <img src="https://flagcdn.com/20x15/<%= flag_code %>.png"
               srcset="https://flagcdn.com/40x30/<%= flag_code %>.png 2x"
               width="20" height="15"
               alt="<%= locale_code.upcase %>"
               class="rounded-xs">
          <span class="font-mono text-[10px] tracking-widest <%= active ? 'text-[#c8a84e]' : 'text-white/40' %>">
            <%= t("locale.#{locale_code}") %>
          </span>
        <% end %>
      <% end %>
    </div>

    <main class="relative z-10 container mx-auto px-3 sm:px-6 max-w-[1100px] pb-8">
      <%= yield %>
    </main>
  </body>
</html>
